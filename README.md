# Guardian Website

## Overview

This is the homepage for SNU Guardian, a student security club at Seoul National University.

## Local preview

Run a static server so `fetch()` works:

```sh
python -m http.server
```

Then open `http://localhost:8000/` in your browser.
